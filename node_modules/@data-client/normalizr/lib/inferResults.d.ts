import type { Schema, NormalizedIndex, EntityTable } from './interface.js';
import type { NormalizeNullable } from './types.js';
/**
 * Build the result parameter to denormalize from schema alone.
 * Tries to compute the entity ids from params.
 */
export default function inferResults<S extends Schema>(schema: S, args: any[], indexes: NormalizedIndex, entities: EntityTable): NormalizeNullable<S>;
export declare function validateInference(results: unknown): any;
//# sourceMappingURL=inferResults.d.ts.map